const answers = require('../docs/data/answers/answers');

exports.seed = function(knex, Promise) {
  // // Begin promises by deleting ALL existing entries
  // const promises = [knex('answers').del()];
  //
  // answers.answers.forEach(el => {
  //   promises.push(knex('answers').insert(el));
  // });
  // 
  // return Promise.all(promises);

};
