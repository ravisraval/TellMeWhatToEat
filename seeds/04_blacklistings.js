const blacklist = require('../docs/data/blacklist/blacklist');

exports.seed = function(knex, Promise) {
  // // Begin promises by deleting ALL existing entries
  // const promises = [knex('blacklist').del()];
  //
  // blacklist.blacklist.forEach(el => {
  //   promises.push(knex('blacklist').insert(el));
  // });
  //
  // return Promise.all(promises);

};
